export const critPowerMod = (critPower) => Math.round(critPower * 0.85);
export const critСhanceMod = (critChance) => Math.round(critChance * 0.85);
export const dodgeMod = (dodge) => Math.round(dodge * 0.85);
export const defMod = (def) => Math.round(def * 0.8);
export const adaptMod = (adapt) => Math.round(adapt * 0.35);
export const vampiricMod = (vampiric) => (vampiric * 0.8) / 100;
